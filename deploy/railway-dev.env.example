# Copy into Railway → dev service → Variables

APP_ENV=development
JWT_SECRET=replace-with-long-random-dev-only-secret
PUBLIC_APP_URL=https://YOUR-DEV-SERVICE.up.railway.app

# Required — attach Postgres + Redis plugins to this service:
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}

# Cloudflare R2 (admission docs + logos)
R2_ENDPOINT=https://fed2231335a1a08237a63ec5f77bc211.r2.cloudflarestorage.com
R2_BUCKET=campusdesk
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# R2_PUBLIC_BASE_URL=https://pub-xxxxx.r2.dev

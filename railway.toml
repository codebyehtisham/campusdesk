# Campus Desk — Railway deploy (production + dev services use this file).
# Create a second Railway service for dev; connect it to the `develop` branch.

[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
